apiVersion: v1
kind: Service
metadata:
  name: "{{ include "helloapp.name" . }}"
  labels:
{{ include "helloapp.labels" . | indent 4 }}
spec:
  ports:
    - port: {{ .Values.port }}
      targetPort: {{ .Values.service.port }}
  selector:
{{ include "helloapp.matchLabels" . | indent 4 }}